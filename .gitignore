# env & secrets
.env
.env.*
!.env.example
.neon

# deps & builds
node_modules/
dist/
server/src/generated/

# native build output (mobile)
mobile/android/build/
mobile/android/app/build/
mobile/android/.gradle/
mobile/modules/**/android/build/
mobile/ios/build/
mobile/.expo/

# dev launcher runtime state (pids, generated runner scripts, logs)
.p2p-run/

# OS / editor cruft
.DS_Store
